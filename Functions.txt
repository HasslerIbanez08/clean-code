1. boolean hasCreditCard(User user) {
    return user.hasCard;
}
2. Circle makeCircle(double x, double y, double radius);
3. public boolean checkMax() {
    return (a < max);
}
checkProfile
isBeneficiary
isPreferredUser
isLoanOrPayment
isAuthenticated
isCostumer


saveUser
getAllUser
deleteUser
